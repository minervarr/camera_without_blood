pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "camera_without_blood"
include ':camera'        // the embeddable camera library (produces the .aar)
include ':engine-audio'  // audio_engine Java, as a standalone coordinate (dedup)
// DEMO placeholder — build.sh swaps this for "include ':demo'" then reverts
